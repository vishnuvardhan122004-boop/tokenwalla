{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "python manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "gunicorn tokenwalla.wsgi --bind 0.0.0.0:$PORT",
    "preDeployCommand": "python manage.py migrate && python manage.py createcachetable tw_cache_table && python manage.py shell -c \"from django.contrib.auth import get_user_model; User = get_user_model(); u, _ = User.objects.get_or_create(mobile='9999999999', defaults={'username': 'admin', 'email': 'admin@tokenwalla.com', 'is_staff': True, 'is_superuser': True}); u.username = 'admin'; u.email = 'admin@tokenwalla.com'; u.is_staff = True; u.is_superuser = True; u.set_password('admin123'); u.save()\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
