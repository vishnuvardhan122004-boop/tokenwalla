{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python manage.py send_appointment_reminders; python manage.py send_pass_expiry_reminders",
    "cronSchedule": "*/10 * * * *",
    "restartPolicyType": "NEVER"
  }
}
