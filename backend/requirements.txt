# ── Django core ───────────────────────────────────────────────────────────────
Django==5.2.1
djangorestframework==3.16.0
djangorestframework_simplejwt==5.4.0
django-cors-headers==4.7.0
whitenoise==6.9.0
gunicorn==23.0.0

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.10
dj-database-url==2.3.0

# ── Storage ───────────────────────────────────────────────────────────────────
cloudinary==1.41.0
django-cloudinary-storage==0.3.0
pillow==11.1.0

# ── Auth / Config ─────────────────────────────────────────────────────────────
python-decouple==3.8
PyJWT==2.10.1

# ── Payments ──────────────────────────────────────────────────────────────────
# Cashfree Payment Gateway SDK (patient checkout). v6 uses API version 2023-08-01,
# class-level auth config, and rupee (not paise) amounts.
cashfree-pg==6.0.1
# Doctor settlement (Cashfree Payouts) needs no SDK — payments/cashfree_payouts_utils.py
# calls the V2 REST API with `requests` below. The cashfree-payout package is a
# V1-era client and was never imported, so it isn't pinned here.
# RSA-OAEP for the Payouts X-Cf-Signature 2FA header (dynamic-IP hosts).
cryptography==44.0.0

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests==2.32.3
urllib3==2.3.0
certifi==2025.1.31
charset-normalizer==3.4.1
idna==3.10

# ── Utilities ─────────────────────────────────────────────────────────────────
asgiref==3.8.1
sqlparse==0.5.3
python-dateutil==2.9.0.post0
six==1.17.0
packaging==24.2
# setuptools provides pkg_resources - needed by some deps on Python 3.14
setuptools>=68.0.0